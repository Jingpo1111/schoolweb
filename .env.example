PORT=3000
SESSION_SECRET=change-this-to-a-long-random-secret
# Local development uses SQLite automatically when DATABASE_URL is empty.
# For public hosting, set DATABASE_URL to a managed PostgreSQL connection string.
DATABASE_URL=
# Set DB_SSL=true only when your hosted PostgreSQL provider requires SSL.
DB_SSL=false
